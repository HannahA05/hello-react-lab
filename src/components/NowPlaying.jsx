function NowPlaying() {
  const currentSong = "Holy Ghost";

  return (
    <div>
      <h2>Now Playing</h2>
      <p>Currently listening to: {currentSong}</p>
    </div>
  );
}

export default NowPlaying;